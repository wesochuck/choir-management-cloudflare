# Evaluate Performance Misses After Event End

Profile Status Automation will not count a Performance as missed until the active Performance has
ended. The evaluator will use the scheduled duration when available and otherwise the end of the
Performance's Organization-local calendar day, preventing a Profile from being penalized while the
event is still underway.
