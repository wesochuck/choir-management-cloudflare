# Use a Time-Based On Break Timeout

The Roster Settings will use a separate enabled-by-default elapsed-time rule to move an
automatically managed On Break Profile to Inactive after a longer configured period, defaulting to
one year (365 calendar days). The rule will not count missed Performances, because a person on break
may not be rostered for events; manual status control exempts a Profile from the timeout.
