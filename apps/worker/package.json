{
  "name": "@choir/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "WRANGLER_LOG_PATH=../../.wrangler/logs wrangler deploy --dry-run --config wrangler.jsonc --env= --outdir dist",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@choir/contracts": "0.1.0",
    "@choir/domain": "0.1.0",
    "better-auth": "1.6.23",
    "hono": "4.13.0",
    "postal-mime": "^2.7.5",
    "zod": "4.4.3"
  }
}
